name: DART_PACKAGE_NAME
description: DESCRIPTION
publish_to: 'none'

version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.5.3"

# TODO remove unnecessary dependencies
dependencies:
  flutter:
    sdk: flutter

  # utopia libs
  utopia_arch: ^0.3.0
  utopia_firebase_crashlytics: ^0.1.0+8
  utopia_hooks: ^0.2.4+5
  utopia_widgets: ^0.0.3
  utopia_utils: ^0.2.6

  # architecture
  flutter_hooks: ^0.18.1
  provider: ^6.0.1
  injector: ^2.0.0

  # firebase
  firebase_core: ^1.10.0

  # platform APIs

  # misc widgets

  # misc utils

  # code generator runtimes
  freezed_annotation: ^1.0.0
  json_annotation: ^4.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

  # code generators
  freezed: ^1.0.0
  json_serializable: ^6.0.1
  build_runner: ^2.1.5
